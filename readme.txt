Smart File Manager
features:
1. list of all folders and sub folders

2. create, delete, rename, open functionality in every folder

3. search
    - global search(search in all sub folders)
    - pattern based search, extention based search, exact match

4. storage
    - max sized folder
    - max size file(100 including subfolders)

5. sorting
    - name, size, ext, ....


1. how much time it takes to find one file in 1lakh files?

fundumentals:
1. exceptions(code should not break any case)
2. proper function names and functionality
3. proper error messages
4. user interactive shell
5. Expected folder count 100, each folder contains 1000 files....
6. use git